export class Tournament{
    // JSON: name
    //name:string;

    // JSON: participants_count
    //participants_count:number;

    // JSON: completed_at
    // Will need to be converted to boolean
    // null -> false
    // not null -> true
    //completed_at: any;
    tournament: any;
}